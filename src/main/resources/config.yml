update_checker: true # Check for new updates

# Teleport to spawn in join
onjoin_spawn: true

# Teleport to the spawn on the first join (Disable "spawn_for_newbies" if you use this)
on_first_join_spawn: false
# Setup different spawn for first join players (Disable "on_first_join_spawn" if you use this)
spawn_for_newbies: true

# Setup different spawn for first join players (Disable "on_first_join_spawn" if you use this)
on_death_spawn: true

prefix: "&8[&bSimpleSetSpawn&8] "
